export interface DashboardSummary {
  totalCamera: number;
  assigned: number;
  fieldVerified: number;
  pendingApproval: number;
  approvedAsBuilt: number;
  conflict: number;
  projectionCursor: number;
}
