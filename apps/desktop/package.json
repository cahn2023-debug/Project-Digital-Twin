{
  "name": "@project/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@project/domain": "workspace:*",
    "@tauri-apps/api": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
