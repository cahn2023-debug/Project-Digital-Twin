import type { Tone } from "../../shared/types";

export const cameraRows: string[][] = [];

export const sourceRows: Array<{ type: string; name: string; meta: string; tone: Tone; state: string }> = [];




